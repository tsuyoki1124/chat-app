<div class="wrapper">
<%# renderで読み込むファイルが同じフォルダにある場合はpartialはいらない。ファイル名のみ %>
  <div class="side-bar">
     <%= render "side_bar" %>
  </div>
  <div class="chat">
    <%= render "main_chat" %>
  </div> 
  </div>